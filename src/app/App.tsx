// Libs
import React, { useState } from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  height: 100%;
`

// Components
export const App = () => {
  return <Wrapper>Goodbye, World</Wrapper>
}
